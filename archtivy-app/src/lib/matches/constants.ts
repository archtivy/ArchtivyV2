/**
 * Display constants for match selection and UI. Does not affect match generation or storage.
 */

/** Minimum score to show a match in the UI (inclusive). */
export const MATCH_MIN_SCORE = 40;

/** Maximum number of matches to show in the lightbox sidebar. */
export const LIGHTBOX_MATCH_LIMIT = 5;
